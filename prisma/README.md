# Prisma

- `schema/` contiene el esquema multiarchivo organizado por módulo propietario.
- `migrations/` conserva una única historia cronológica.
- `seed/` contiene el orquestador idempotente y las categorías aprobadas.

Los modelos de negocio se añadirán únicamente al implementar el módulo propietario.
