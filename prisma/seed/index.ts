const seeders = ['permissions', 'roles', 'admin', 'settings'] as const;

function main(): void {
  console.info(`No infrastructure seed data is required yet (${seeders.join(', ')} reserved).`);
}

main();
